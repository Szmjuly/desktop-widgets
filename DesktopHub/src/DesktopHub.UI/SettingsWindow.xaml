<Window x:Class="DesktopHub.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DesktopHub Settings"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Width="750"
        Height="650"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown">
    
    <Window.Resources>
        <!-- Custom thin rounded scrollbar style -->
        <Style x:Key="ThinScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="8"/>
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True" Width="8">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Background="#60F5F7FA" 
                                                        CornerRadius="4"
                                                        Width="8"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ThinScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                     Grid.Column="1"
                                     Value="{TemplateBinding VerticalOffset}"
                                     Maximum="{TemplateBinding ScrollableHeight}"
                                     ViewportSize="{TemplateBinding ViewportHeight}"
                                     Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                     Style="{StaticResource ThinScrollBar}"
                                     Margin="4,0,0,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- Glass effect container -->
    <Border x:Name="RootBorder" 
            Background="#C8181818" 
            CornerRadius="12" 
            BorderBrush="#442A2A2A" 
            BorderThickness="1">
        <Grid>
            <!-- Inner glass overlay for depth -->
            <Border x:Name="GlassOverlay"
                    Margin="2"
                    CornerRadius="10"
                    BorderThickness="1"
                    BorderBrush="#33FFFFFF"
                    Background="#14000000"
                    IsHitTestVisible="False"/>
            
            <!-- Content -->
            <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header (draggable) -->
            <Grid Grid.Row="0" Margin="0,0,0,20" Background="Transparent" MouseDown="Header_MouseDown" Cursor="Hand">
                <TextBlock Text="Settings" 
                           FontSize="24" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}"/>
                <Button x:Name="CloseButton"
                        Content="✕"
                        Width="32"
                        Height="32"
                        HorizontalAlignment="Right"
                        Background="#22FFFFFF"
                        Foreground="{StaticResource TextBrush}"
                        BorderBrush="#22FFFFFF"
                        Click="CloseButton_Click"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="1"
                                                CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <!-- Side Menu Layout -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Side Navigation Menu -->
                <Border Grid.Column="0" Background="#18FFFFFF" CornerRadius="8" Padding="8" Margin="0,0,12,0">
                    <StackPanel x:Name="NavigationMenu">
                        <RadioButton x:Name="ShortcutsMenuButton" Content="⚡ Shortcuts" IsChecked="True" Checked="MenuButton_Checked" 
                                     GroupName="SettingsMenu" Margin="0,0,0,8" Padding="12,10" FontSize="14" FontWeight="Medium" 
                                     Foreground="{StaticResource TextBrush}" Cursor="Hand">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="RadioButton">
                                                <Border x:Name="Border" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                        
                        <RadioButton x:Name="AppearanceMenuButton" Content="🎨 Appearance" Checked="MenuButton_Checked"
                                     GroupName="SettingsMenu" Margin="0,0,0,8" Padding="12,10" FontSize="14" FontWeight="Medium"
                                     Foreground="{StaticResource TextBrush}" Cursor="Hand">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="RadioButton">
                                                <Border x:Name="Border" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                        
                        <RadioButton x:Name="GeneralMenuButton" Content="⚙️ General" Checked="MenuButton_Checked"
                                     GroupName="SettingsMenu" Margin="0,0,0,8" Padding="12,10" FontSize="14" FontWeight="Medium"
                                     Foreground="{StaticResource TextBrush}" Cursor="Hand">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="RadioButton">
                                                <Border x:Name="Border" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                        
                        <RadioButton x:Name="QuickTasksMenuButton" Content="☑ Quick Tasks" Checked="MenuButton_Checked"
                                     GroupName="SettingsMenu" Margin="0,0,0,8" Padding="12,10" FontSize="14" FontWeight="Medium"
                                     Foreground="{StaticResource TextBrush}" Cursor="Hand">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="RadioButton">
                                                <Border x:Name="Border" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                        
                        <RadioButton x:Name="DocQuickOpenMenuButton" Content="📄 Doc Quick Open" Checked="MenuButton_Checked"
                                     GroupName="SettingsMenu" Margin="0,0,0,8" Padding="12,10" FontSize="14" FontWeight="Medium"
                                     Foreground="{StaticResource TextBrush}" Cursor="Hand">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="RadioButton">
                                                <Border x:Name="Border" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                    </StackPanel>
                </Border>
                
                <!-- Content Area -->
                <Grid Grid.Column="1">
                    <!-- Shortcuts Panel -->
                    <Border x:Name="ShortcutsPanel" Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="20" Visibility="Visible">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ThinScrollViewer}">
                            <StackPanel>
                    <!-- Hotkey Section -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Global Hotkey" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            
                            <TextBlock Text="Press a new key combination to change the hotkey" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,12"/>
                            
                            <!-- Hotkey Display -->
                            <Border x:Name="HotkeyBox"
                                    Background="#22FFFFFF"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="12"
                                    Margin="0,0,0,12"
                                    Cursor="Hand"
                                    MouseDown="HotkeyBox_MouseDown">
                                <Grid>
                                    <TextBlock x:Name="HotkeyText"
                                               Text="Ctrl+Alt+Space"
                                               FontSize="14"
                                               FontWeight="Medium"
                                               Foreground="{StaticResource TextBrush}"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="RecordingText"
                                               Text="Press a key combination..."
                                               FontSize="14"
                                               FontStyle="Italic"
                                               Foreground="{StaticResource PrimaryBrush}"
                                               HorizontalAlignment="Center"
                                               Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            
                            <Button x:Name="ResetHotkeyButton"
                                    Content="Reset to Default (Ctrl+Alt+Space)"
                                    Padding="12,8"
                                    Background="#22FFFFFF"
                                    Foreground="{StaticResource TextSecondaryBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    Click="ResetHotkeyButton_Click"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                                            BorderThickness="1"
                                                            CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Border x:Name="HotkeyTypingWarning"
                                    Background="#332B1B00"
                                    BorderBrush="#665A3A00"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10"
                                    Margin="0,12,0,0"
                                    Visibility="Collapsed">
                                <TextBlock x:Name="HotkeyTypingWarningText"
                                           TextWrapping="Wrap"
                                           FontSize="12"
                                           Foreground="#F3D6A2"/>
                            </Border>
                        </StackPanel>
                    </Border>
                    
                    <!-- Close Shortcut Section -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Widget Close Shortcut" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            
                            <TextBlock Text="Shortcut key to close overlay and widget windows" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,12"/>
                            
                            <!-- Close Shortcut Display -->
                            <Border x:Name="CloseShortcutBox"
                                    Background="#22FFFFFF"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="12"
                                    Margin="0,0,0,12"
                                    Cursor="Hand"
                                    MouseDown="CloseShortcutBox_MouseDown">
                                <Grid>
                                    <TextBlock x:Name="CloseShortcutText"
                                               Text="Esc"
                                               FontSize="14"
                                               FontWeight="Medium"
                                               Foreground="{StaticResource TextBrush}"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="CloseShortcutRecordingText"
                                               Text="Press a key combination..."
                                               FontSize="14"
                                               FontStyle="Italic"
                                               Foreground="{StaticResource PrimaryBrush}"
                                               HorizontalAlignment="Center"
                                               Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            
                            <Button x:Name="ResetCloseShortcutButton"
                                    Content="Reset to Default (Esc)"
                                    Padding="12,8"
                                    Background="#22FFFFFF"
                                    Foreground="{StaticResource TextSecondaryBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    Click="ResetCloseShortcutButton_Click"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                                            BorderThickness="1"
                                                            CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Border x:Name="CloseShortcutTypingWarning"
                                    Background="#332B1B00"
                                    BorderBrush="#665A3A00"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="10"
                                    Margin="0,12,0,0"
                                    Visibility="Collapsed">
                                <TextBlock x:Name="CloseShortcutTypingWarningText"
                                           TextWrapping="Wrap"
                                           FontSize="12"
                                           Foreground="#F3D6A2"/>
                            </Border>
                        </StackPanel>
                    </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    
                    <!-- Appearance Panel -->
                    <Border x:Name="AppearancePanel" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="20" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ThinScrollViewer}">
                            <StackPanel>
                    
                    <!-- Window Transparency Section -->
                    <Border Background="#18FFFFFF" 
                            BorderBrush="#22FFFFFF"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                        <StackPanel>
                            <TextBlock Text="Window Transparency" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,12"/>
                            
                            <!-- Settings Window Transparency -->
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Settings Window" 
                                           FontSize="13" 
                                           FontWeight="Medium"
                                           Foreground="{StaticResource TextBrush}"
                                           VerticalAlignment="Center"/>
                                
                                <Button x:Name="LinkSettingsButton"
                                        HorizontalAlignment="Right"
                                        Width="24"
                                        Height="24"
                                        Background="#22FFFFFF"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        Click="LinkSettingsButton_Click"
                                        Cursor="Hand"
                                        ToolTip="Link to other sliders">
                                    <Grid>
                                        <TextBlock Text="�" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                            
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0"
                                          Text="Transparent"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>
                                
                                <Slider x:Name="SettingsTransparencySlider"
                                       Grid.Column="1"
                                       Minimum="0.4"
                                       Maximum="0.95"
                                       Value="0.78"
                                       TickFrequency="0.05"
                                       IsSnapToTickEnabled="True"
                                       ValueChanged="SettingsTransparencySlider_ValueChanged"
                                       VerticalAlignment="Center">
                                    <Slider.Template>
                                        <ControlTemplate TargetType="Slider">
                                            <Grid>
                                                <Border Background="#40FFFFFF" 
                                                        Height="4" 
                                                        CornerRadius="2"
                                                        VerticalAlignment="Center"/>
                                                <Track x:Name="PART_Track">
                                                    <Track.DecreaseRepeatButton>
                                                        <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.DecreaseRepeatButton>
                                                    <Track.IncreaseRepeatButton>
                                                        <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.IncreaseRepeatButton>
                                                    <Track.Thumb>
                                                        <Thumb>
                                                            <Thumb.Template>
                                                                <ControlTemplate>
                                                                    <Ellipse Fill="{StaticResource PrimaryBrush}"
                                                                            Width="16"
                                                                            Height="16"
                                                                            Stroke="White"
                                                                            StrokeThickness="2"/>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Slider.Template>
                                </Slider>
                                
                                <TextBlock Grid.Column="2"
                                          Text="Opaque"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="8,0,0,0"/>
                            </Grid>
                            
                            <!-- Search Overlay Transparency -->
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Search Overlay" 
                                           FontSize="13" 
                                           FontWeight="Medium"
                                           Foreground="{StaticResource TextBrush}"
                                           VerticalAlignment="Center"/>
                                
                                <Button x:Name="LinkOverlayButton"
                                        HorizontalAlignment="Right"
                                        Width="24"
                                        Height="24"
                                        Background="#22FFFFFF"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        Click="LinkOverlayButton_Click"
                                        Cursor="Hand"
                                        ToolTip="Link to other sliders">
                                    <Grid>
                                        <TextBlock Text="�" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0"
                                          Text="Transparent"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>
                                
                                <Slider x:Name="OverlayTransparencySlider"
                                       Grid.Column="1"
                                       Minimum="0.4"
                                       Maximum="0.95"
                                       Value="0.78"
                                       TickFrequency="0.05"
                                       IsSnapToTickEnabled="True"
                                       ValueChanged="OverlayTransparencySlider_ValueChanged"
                                       VerticalAlignment="Center">
                                    <Slider.Template>
                                        <ControlTemplate TargetType="Slider">
                                            <Grid>
                                                <Border Background="#40FFFFFF" 
                                                        Height="4" 
                                                        CornerRadius="2"
                                                        VerticalAlignment="Center"/>
                                                <Track x:Name="PART_Track">
                                                    <Track.DecreaseRepeatButton>
                                                        <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.DecreaseRepeatButton>
                                                    <Track.IncreaseRepeatButton>
                                                        <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.IncreaseRepeatButton>
                                                    <Track.Thumb>
                                                        <Thumb>
                                                            <Thumb.Template>
                                                                <ControlTemplate>
                                                                    <Ellipse Fill="{StaticResource PrimaryBrush}"
                                                                            Width="16"
                                                                            Height="16"
                                                                            Stroke="White"
                                                                            StrokeThickness="2"/>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Slider.Template>
                                </Slider>
                                
                                <TextBlock Grid.Column="2"
                                          Text="Opaque"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="8,0,0,0"/>
                            </Grid>
                            
                            <!-- Widget Launcher Transparency -->
                            <Grid Margin="0,16,0,8">
                                <TextBlock Text="Widget Launcher" 
                                           FontSize="13" 
                                           FontWeight="Medium"
                                           Foreground="{StaticResource TextBrush}"
                                           VerticalAlignment="Center"/>
                                
                                <Button x:Name="LinkLauncherButton"
                                        HorizontalAlignment="Right"
                                        Width="24"
                                        Height="24"
                                        Background="#22FFFFFF"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        Click="LinkLauncherButton_Click"
                                        Cursor="Hand"
                                        ToolTip="Link to other sliders">
                                    <Grid>
                                        <TextBlock Text="�" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                            
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0"
                                          Text="Transparent"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>
                                
                                <Slider x:Name="WidgetLauncherTransparencySlider"
                                       Grid.Column="1"
                                       Minimum="0.4"
                                       Maximum="0.95"
                                       Value="0.78"
                                       TickFrequency="0.05"
                                       IsSnapToTickEnabled="True"
                                       ValueChanged="WidgetLauncherTransparencySlider_ValueChanged"
                                       VerticalAlignment="Center">
                                    <Slider.Template>
                                        <ControlTemplate TargetType="Slider">
                                            <Grid>
                                                <Border Background="#40FFFFFF" 
                                                        Height="4" 
                                                        CornerRadius="2"
                                                        VerticalAlignment="Center"/>
                                                <Track x:Name="PART_Track">
                                                    <Track.DecreaseRepeatButton>
                                                        <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.DecreaseRepeatButton>
                                                    <Track.IncreaseRepeatButton>
                                                        <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.IncreaseRepeatButton>
                                                    <Track.Thumb>
                                                        <Thumb>
                                                            <Thumb.Template>
                                                                <ControlTemplate>
                                                                    <Ellipse Fill="{StaticResource PrimaryBrush}"
                                                                            Width="16"
                                                                            Height="16"
                                                                            Stroke="White"
                                                                            StrokeThickness="2"/>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Slider.Template>
                                </Slider>
                                
                                <TextBlock Grid.Column="2"
                                          Text="Opaque"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="8,0,0,0"/>
                            </Grid>
                            
                            <!-- Timer Widget Transparency -->
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Timer Widget" 
                                           FontSize="13" 
                                           FontWeight="Medium"
                                           Foreground="{StaticResource TextBrush}"
                                           VerticalAlignment="Center"/>
                                
                                <Button x:Name="LinkTimerButton"
                                        HorizontalAlignment="Right"
                                        Width="24"
                                        Height="24"
                                        Background="#22FFFFFF"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        Click="LinkTimerButton_Click"
                                        Cursor="Hand"
                                        ToolTip="Link to other sliders">
                                    <Grid>
                                        <TextBlock Text="�" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0"
                                          Text="Transparent"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,8,0"/>
                                
                                <Slider x:Name="TimerWidgetTransparencySlider"
                                       Grid.Column="1"
                                       Minimum="0.4"
                                       Maximum="0.95"
                                       Value="0.78"
                                       TickFrequency="0.05"
                                       IsSnapToTickEnabled="True"
                                       ValueChanged="TimerWidgetTransparencySlider_ValueChanged"
                                       VerticalAlignment="Center">
                                    <Slider.Template>
                                        <ControlTemplate TargetType="Slider">
                                            <Grid>
                                                <Border Background="#40FFFFFF" 
                                                        Height="4" 
                                                        CornerRadius="2"
                                                        VerticalAlignment="Center"/>
                                                <Track x:Name="PART_Track">
                                                    <Track.DecreaseRepeatButton>
                                                        <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.DecreaseRepeatButton>
                                                    <Track.IncreaseRepeatButton>
                                                        <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.IncreaseRepeatButton>
                                                    <Track.Thumb>
                                                        <Thumb>
                                                            <Thumb.Template>
                                                                <ControlTemplate>
                                                                    <Ellipse Fill="{StaticResource PrimaryBrush}"
                                                                            Width="16"
                                                                            Height="16"
                                                                            Stroke="White"
                                                                            StrokeThickness="2"/>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Slider.Template>
                                </Slider>
                                
                                <TextBlock Grid.Column="2"
                                          Text="Opaque"
                                          FontSize="10"
                                          Foreground="{StaticResource TextSecondaryBrush}"
                                          VerticalAlignment="Center"
                                          Margin="8,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    
                    <!-- General Panel -->
                    <Border x:Name="GeneralPanel" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="20" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ThinScrollViewer}">
                            <StackPanel>
                    
                    <!-- Auto-start Section -->
                    <Border Background="#18FFFFFF" 
                            BorderBrush="#22FFFFFF"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Run at Startup" 
                                           FontSize="16" 
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Launch DesktopHub automatically when Windows starts" 
                                           FontSize="12" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                            
                            <ToggleButton x:Name="AutoStartToggle"
                                          Grid.Column="1"
                                          Width="50"
                                          Height="28"
                                          VerticalAlignment="Center"
                                          Checked="AutoStartToggle_Checked"
                                          Unchecked="AutoStartToggle_Unchecked"
                                          Cursor="Hand">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="Border"
                                                            Background="#333333"
                                                            CornerRadius="14"
                                                            Width="50"
                                                            Height="28">
                                                        <Ellipse x:Name="Thumb"
                                                                 Width="20"
                                                                 Height="20"
                                                                 Fill="White"
                                                                 HorizontalAlignment="Left"
                                                                 Margin="4,0,0,0"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                            <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </Grid>
                    </Border>
                    
                    <!-- Network Drives Section -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Network Drives" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            
                            <TextBlock Text="Configure which network drives to scan for projects" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,16"/>
                            
                            <!-- Q Drive -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <ToggleButton x:Name="QDriveEnabledToggle"
                                              Grid.Column="0"
                                              Width="50"
                                              Height="28"
                                              VerticalAlignment="Bottom"
                                              Margin="0,0,12,4"
                                              Checked="QDriveEnabledToggle_Checked"
                                              Unchecked="QDriveEnabledToggle_Unchecked"
                                              Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border"
                                                                Background="#333333"
                                                                CornerRadius="14"
                                                                Width="50"
                                                                Height="28">
                                                            <Ellipse x:Name="Thumb"
                                                                     Width="20"
                                                                     Height="20"
                                                                     Fill="White"
                                                                     HorizontalAlignment="Left"
                                                                     Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Q: Drive (Florida)" 
                                               FontSize="14" 
                                               FontWeight="Medium"
                                               Foreground="{StaticResource TextBrush}"
                                               Margin="0,0,0,4"/>
                                    <TextBox x:Name="QDrivePathBox"
                                             Text="Q:\"
                                             FontSize="12"
                                             Padding="6"
                                             Background="#22FFFFFF"
                                             Foreground="{StaticResource TextBrush}"
                                             BorderBrush="{StaticResource BorderBrush}"
                                             TextChanged="QDrivePathBox_TextChanged">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>
                            </Grid>
                            
                            <!-- P Drive -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <ToggleButton x:Name="PDriveEnabledToggle"
                                              Grid.Column="0"
                                              Width="50"
                                              Height="28"
                                              VerticalAlignment="Bottom"
                                              Margin="0,0,12,4"
                                              Checked="PDriveEnabledToggle_Checked"
                                              Unchecked="PDriveEnabledToggle_Unchecked"
                                              Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border"
                                                                Background="#333333"
                                                                CornerRadius="14"
                                                                Width="50"
                                                                Height="28">
                                                            <Ellipse x:Name="Thumb"
                                                                     Width="20"
                                                                     Height="20"
                                                                     Fill="White"
                                                                     HorizontalAlignment="Left"
                                                                     Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="P: Drive (Connecticut)" 
                                               FontSize="14" 
                                               FontWeight="Medium"
                                               Foreground="{StaticResource TextBrush}"
                                               Margin="0,0,0,4"/>
                                    <TextBox x:Name="PDrivePathBox"
                                             Text="P:\"
                                             FontSize="12"
                                             Padding="6"
                                             Background="#22FFFFFF"
                                             Foreground="{StaticResource TextBrush}"
                                             BorderBrush="{StaticResource BorderBrush}"
                                             TextChanged="PDrivePathBox_TextChanged">
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Living Widgets Mode Section -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Living Widgets Mode" 
                                           FontSize="16" 
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,4"/>
                                <TextBlock FontSize="12" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextWrapping="Wrap">
                                    <Run Text="Make widgets draggable, snappable to screen edges, and pinnable to desktop."/>
                                    <LineBreak/>
                                    <Run Text="When enabled, widgets stay visible until manually closed with hotkey."/>
                                    <LineBreak/>
                                    <Run Text="When disabled (default), widgets auto-hide when clicking away."/>
                                </TextBlock>
                            </StackPanel>
                            
                            <ToggleButton x:Name="LivingWidgetsModeToggle"
                                          Grid.Column="1"
                                          Width="50"
                                          Height="28"
                                          VerticalAlignment="Center"
                                          Checked="LivingWidgetsModeToggle_Checked"
                                          Unchecked="LivingWidgetsModeToggle_Unchecked"
                                          Cursor="Hand">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="Border"
                                                            Background="#333333"
                                                            CornerRadius="14"
                                                            Width="50"
                                                            Height="28">
                                                        <Ellipse x:Name="Thumb"
                                                                 Width="20"
                                                                 Height="20"
                                                                 Fill="White"
                                                                 HorizontalAlignment="Left"
                                                                 Margin="4,0,0,0"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                            <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </Grid>
                    </Border>
                    
                    <!-- Notification Duration Section -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                        <StackPanel>
                            <TextBlock Text="Notification Duration" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            
                            <TextBlock Text="How long balloon notifications stay visible" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,0,12"/>
                            
                            <!-- Radio buttons for preset durations -->
                            <StackPanel x:Name="NotificationDurationStack" Margin="0,0,0,8">
                                <RadioButton x:Name="NotificationShortRadio"
                                             Content="Short (2 seconds)"
                                             FontSize="13"
                                             Foreground="{StaticResource TextBrush}"
                                             Margin="0,0,0,4"
                                             Checked="NotificationDurationRadio_Checked"/>
                                
                                <RadioButton x:Name="NotificationMediumRadio"
                                             Content="Medium (3 seconds)"
                                             FontSize="13"
                                             Foreground="{StaticResource TextBrush}"
                                             Margin="0,0,0,4"
                                             Checked="NotificationDurationRadio_Checked"/>
                                
                                <RadioButton x:Name="NotificationLongRadio"
                                             Content="Long (5 seconds)"
                                             FontSize="13"
                                             Foreground="{StaticResource TextBrush}"
                                             Margin="0,0,0,4"
                                             Checked="NotificationDurationRadio_Checked"/>
                                
                                <RadioButton x:Name="NotificationDisabledRadio"
                                             Content="Disabled (no notifications)"
                                             FontSize="13"
                                             Foreground="{StaticResource TextBrush}"
                                             Checked="NotificationDurationRadio_Checked"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    
                    <!-- Quick Tasks Panel -->
                    <Border x:Name="QuickTasksPanel" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="20" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ThinScrollViewer}">
                            <StackPanel>
                    
                    <!-- Display Settings -->
                    <Border Background="#18FFFFFF" 
                            BorderBrush="#22FFFFFF"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Display" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,12"/>
                            
                            <!-- Show Completed Tasks -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Show Completed Tasks" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Display completed tasks in the list (with strikethrough)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleButton x:Name="QT_ShowCompletedToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="QT_ShowCompletedToggle_Changed" Unchecked="QT_ShowCompletedToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                            
                            <!-- Compact Mode -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Compact Mode" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Tighter spacing to fit more tasks on screen" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleButton x:Name="QT_CompactModeToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="QT_CompactModeToggle_Changed" Unchecked="QT_CompactModeToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                            
                            <!-- Completed Task Opacity -->
                            <TextBlock Text="Completed Task Opacity" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Faded" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <Slider x:Name="QT_CompletedOpacitySlider" Grid.Column="1" Minimum="0.2" Maximum="1.0" Value="0.5"
                                        TickFrequency="0.1" IsSnapToTickEnabled="True" ValueChanged="QT_CompletedOpacitySlider_Changed" VerticalAlignment="Center">
                                    <Slider.Template>
                                        <ControlTemplate TargetType="Slider">
                                            <Grid>
                                                <Border Background="#40FFFFFF" Height="4" CornerRadius="2" VerticalAlignment="Center"/>
                                                <Track x:Name="PART_Track">
                                                    <Track.DecreaseRepeatButton><RepeatButton Command="Slider.DecreaseLarge" Opacity="0" IsHitTestVisible="False"/></Track.DecreaseRepeatButton>
                                                    <Track.IncreaseRepeatButton><RepeatButton Command="Slider.IncreaseLarge" Opacity="0" IsHitTestVisible="False"/></Track.IncreaseRepeatButton>
                                                    <Track.Thumb>
                                                        <Thumb>
                                                            <Thumb.Template>
                                                                <ControlTemplate>
                                                                    <Ellipse Fill="{StaticResource PrimaryBrush}" Width="16" Height="16" Stroke="White" StrokeThickness="2"/>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Slider.Template>
                                </Slider>
                                <TextBlock Grid.Column="2" Text="Solid" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Behavior Settings -->
                    <Border Background="#18FFFFFF" 
                            BorderBrush="#22FFFFFF"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Behavior" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,12"/>
                            
                            <!-- Auto Carry-Over -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Auto Carry-Over" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Automatically carry incomplete tasks to the next day" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                                </StackPanel>
                                <ToggleButton x:Name="QT_AutoCarryOverToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="QT_AutoCarryOverToggle_Changed" Unchecked="QT_AutoCarryOverToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                            
                            <!-- Default Priority -->
                            <TextBlock Text="Default Priority" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                            <TextBlock Text="Priority assigned to new tasks" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                            <StackPanel x:Name="QT_DefaultPriorityStack" Orientation="Horizontal" Margin="0,0,0,16">
                                <RadioButton x:Name="QT_PriorityLow" Content="Low" GroupName="QTPriority" FontSize="12" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="QT_DefaultPriority_Changed" Cursor="Hand"/>
                                <RadioButton x:Name="QT_PriorityNormal" Content="Normal" GroupName="QTPriority" FontSize="12" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="QT_DefaultPriority_Changed" Cursor="Hand" IsChecked="True"/>
                                <RadioButton x:Name="QT_PriorityHigh" Content="High" GroupName="QTPriority" FontSize="12" Foreground="{StaticResource TextBrush}" Checked="QT_DefaultPriority_Changed" Cursor="Hand"/>
                            </StackPanel>
                            
                            <!-- Sort Mode -->
                            <TextBlock Text="Sort Order" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                            <TextBlock Text="How tasks are ordered in the list" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                            <StackPanel x:Name="QT_SortModeStack" Orientation="Horizontal" Margin="0,0,0,4">
                                <RadioButton x:Name="QT_SortManual" Content="Manual" GroupName="QTSort" FontSize="12" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="QT_SortMode_Changed" Cursor="Hand" IsChecked="True"/>
                                <RadioButton x:Name="QT_SortPriority" Content="Priority" GroupName="QTSort" FontSize="12" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="QT_SortMode_Changed" Cursor="Hand"/>
                                <RadioButton x:Name="QT_SortCreated" Content="Created" GroupName="QTSort" FontSize="12" Foreground="{StaticResource TextBrush}" Checked="QT_SortMode_Changed" Cursor="Hand"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Categories -->
                    <Border Background="#18FFFFFF" 
                            BorderBrush="#22FFFFFF"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                        <StackPanel>
                            <TextBlock Text="Categories" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       Margin="0,0,0,4"/>
                            <TextBlock Text="Right-click a task to assign a category" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,12"/>
                            
                            <StackPanel x:Name="QT_CategoriesList" Margin="0,0,0,8"/>
                            
                            <!-- Add Category -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="QT_NewCategoryInput" Grid.Column="0" FontSize="12" Padding="6" 
                                         Background="#22FFFFFF" Foreground="{StaticResource TextBrush}" BorderBrush="{StaticResource BorderBrush}"
                                         KeyDown="QT_NewCategoryInput_KeyDown">
                                    <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></TextBox.Resources>
                                </TextBox>
                                <Button Grid.Column="1" Content="+ Add" Padding="10,6" Margin="8,0,0,0"
                                        Background="#22FFFFFF" Foreground="{StaticResource TextBrush}" BorderBrush="{StaticResource BorderBrush}"
                                        Click="QT_AddCategory_Click" Cursor="Hand">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    
                    <!-- Doc Quick Open Panel -->
                    <Border x:Name="DocQuickOpenPanel" Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="20" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ThinScrollViewer}">
                            <StackPanel>
                    
                    <!-- Display Settings -->
                    <Border Background="#18FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Display" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                            
                            <!-- Show File Size -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Show File Size" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Display file size next to each file" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleButton x:Name="DQ_ShowFileSizeToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="DQ_ShowFileSizeToggle_Changed" Unchecked="DQ_ShowFileSizeToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                            
                            <!-- Show Date Modified -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Show Date Modified" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Display last modified date for each file" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleButton x:Name="DQ_ShowDateModifiedToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="DQ_ShowDateModifiedToggle_Changed" Unchecked="DQ_ShowDateModifiedToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                            
                            <!-- Show File Extension -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Show File Extension" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Show .pdf, .dwg etc. in file names" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleButton x:Name="DQ_ShowFileExtToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="DQ_ShowFileExtToggle_Changed" Unchecked="DQ_ShowFileExtToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                            
                            <!-- Compact Mode -->
                            <Grid Margin="0,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Compact Mode" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Tighter row spacing for more files on screen" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleButton x:Name="DQ_CompactModeToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="DQ_CompactModeToggle_Changed" Unchecked="DQ_CompactModeToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Scanning Settings -->
                    <Border Background="#18FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Scanning" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                            
                            <!-- Max Scan Depth -->
                            <StackPanel Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Max Scan Depth" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock x:Name="DQ_MaxDepthValue" Grid.Column="1" Text="3" FontSize="13" Foreground="{StaticResource AccentBrush}"/>
                                </Grid>
                                <TextBlock Text="How many subfolder levels deep to scan (1-5)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,6"/>
                                <Slider x:Name="DQ_MaxDepthSlider" Minimum="1" Maximum="5" TickFrequency="1" IsSnapToTickEnabled="True"
                                        ValueChanged="DQ_MaxDepthSlider_Changed" Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>
                            
                            <!-- Max Files -->
                            <StackPanel Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Max Files" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock x:Name="DQ_MaxFilesValue" Grid.Column="1" Text="200" FontSize="13" Foreground="{StaticResource AccentBrush}"/>
                                </Grid>
                                <TextBlock Text="Maximum number of files to display (50-500)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,6"/>
                                <Slider x:Name="DQ_MaxFilesSlider" Minimum="50" Maximum="500" TickFrequency="50" IsSnapToTickEnabled="True"
                                        ValueChanged="DQ_MaxFilesSlider_Changed" Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>
                            
                            <!-- File Extensions -->
                            <TextBlock Text="File Extensions" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="Comma-separated list of extensions to scan" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                            <TextBox x:Name="DQ_ExtensionsInput" FontSize="12" Padding="8,6" TextWrapping="Wrap" MinHeight="48"
                                     Background="#22FFFFFF" Foreground="{StaticResource TextBrush}" BorderBrush="{StaticResource BorderBrush}"
                                     LostFocus="DQ_ExtensionsInput_LostFocus">
                                <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></TextBox.Resources>
                            </TextBox>
                        </StackPanel>
                    </Border>
                    
                    <!-- Sorting & Grouping -->
                    <Border Background="#18FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Sorting &amp; Grouping" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                            
                            <!-- Sort By -->
                            <TextBlock Text="Sort By" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <RadioButton x:Name="DQ_SortName" Content="Name" GroupName="DQSort" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="DQ_SortChanged" Cursor="Hand"/>
                                <RadioButton x:Name="DQ_SortDate" Content="Date" GroupName="DQSort" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="DQ_SortChanged" Cursor="Hand"/>
                                <RadioButton x:Name="DQ_SortType" Content="Type" GroupName="DQSort" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="DQ_SortChanged" Cursor="Hand"/>
                                <RadioButton x:Name="DQ_SortSize" Content="Size" GroupName="DQSort" Foreground="{StaticResource TextBrush}" Checked="DQ_SortChanged" Cursor="Hand"/>
                            </StackPanel>
                            
                            <!-- Group By -->
                            <TextBlock Text="Group By" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                <RadioButton x:Name="DQ_GroupNone" Content="None" GroupName="DQGroup" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="DQ_GroupChanged" Cursor="Hand"/>
                                <RadioButton x:Name="DQ_GroupCategory" Content="Category" GroupName="DQGroup" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="DQ_GroupChanged" Cursor="Hand"/>
                                <RadioButton x:Name="DQ_GroupExt" Content="Extension" GroupName="DQGroup" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0" Checked="DQ_GroupChanged" Cursor="Hand"/>
                                <RadioButton x:Name="DQ_GroupSubfolder" Content="Subfolder" GroupName="DQGroup" Foreground="{StaticResource TextBrush}" Checked="DQ_GroupChanged" Cursor="Hand"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Behavior -->
                    <Border Background="#18FFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Behavior" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
                            
                            <!-- Auto-open last project -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Remember Last Project" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}" Margin="0,0,0,2"/>
                                    <TextBlock Text="Auto-open the last selected project on launch" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleButton x:Name="DQ_AutoOpenToggle" Grid.Column="1" Width="50" Height="28" VerticalAlignment="Center"
                                              Checked="DQ_AutoOpenToggle_Changed" Unchecked="DQ_AutoOpenToggle_Changed" Cursor="Hand">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="Border" Background="#333333" CornerRadius="14" Width="50" Height="28">
                                                            <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                            
                            <!-- Recent Files Count -->
                            <StackPanel Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Recent Files Count" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock x:Name="DQ_RecentCountValue" Grid.Column="1" Text="10" FontSize="13" Foreground="{StaticResource AccentBrush}"/>
                                </Grid>
                                <TextBlock Text="Number of recently opened files to remember (0 = disabled)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,6"/>
                                <Slider x:Name="DQ_RecentCountSlider" Minimum="0" Maximum="20" TickFrequency="1" IsSnapToTickEnabled="True"
                                        ValueChanged="DQ_RecentCountSlider_Changed" Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>
                            
                            <!-- Transparency -->
                            <StackPanel Margin="0,0,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Widget Transparency" FontSize="13" FontWeight="Medium" Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock x:Name="DQ_TransparencyValue" Grid.Column="1" Text="78%" FontSize="13" Foreground="{StaticResource AccentBrush}"/>
                                </Grid>
                                <TextBlock Text="Adjust the widget background transparency" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,6"/>
                                <Slider x:Name="DQ_TransparencySlider" Minimum="0.3" Maximum="1.0" TickFrequency="0.01" IsSnapToTickEnabled="True"
                                        ValueChanged="DQ_TransparencySlider_Changed" Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>
            
            <!-- Footer -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <TextBlock x:Name="StatusText"
                           Text="Settings are saved automatically"
                           FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Grid>
        </Grid>
    </Border>
</Window>
