<UserControl x:Class="DesktopHub.UI.Widgets.SmartProjectSearchWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Stretch">

    <!-- Brushes and styles inherited from App.xaml theme resources -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Smart Project Search"
                      FontSize="14"
                      FontWeight="SemiBold"
                      Foreground="{DynamicResource TextBrush}"/>
            <TextBlock x:Name="ProjectLabel"
                      Text="No project selected"
                      FontSize="11"
                      Foreground="{DynamicResource AccentBrush}"
                      TextTrimming="CharacterEllipsis"
                      Margin="0,2,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="SearchBox"
                    Grid.Row="0"
                    Padding="10,6"
                    FontSize="12"
                    Background="{DynamicResource CardBorderBrush}"
                    Foreground="{DynamicResource TextBrush}"
                    CaretBrush="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource HoverStrongBrush}"
                    TextChanged="SearchBox_TextChanged"
                    PreviewKeyDown="SearchBox_PreviewKeyDown"/>

            <TextBlock x:Name="HintText"
                      Grid.Row="1"
                      Text="Try: fault current letter | fpl::pdf | fpl::pdf|word | latest fault current letter"
                      FontSize="10"
                      Foreground="{DynamicResource TextSecondaryBrush}"
                      Margin="2,6,2,0"
                      TextWrapping="Wrap"/>
        </Grid>

        <ListBox x:Name="ResultsList"
                Grid.Row="2"
                Background="{DynamicResource FaintOverlayBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                Foreground="{DynamicResource TextBrush}"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.CanContentScroll="True"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Recycling"
                MouseDoubleClick="ResultsList_MouseDoubleClick"
                PreviewMouseRightButtonUp="ResultsList_PreviewMouseRightButtonUp"
                KeyDown="ResultsList_KeyDown">
            <ListBox.Template>
                <ControlTemplate TargetType="ListBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer Style="{StaticResource ThinScrollViewer}" Focusable="False" Padding="{TemplateBinding Padding}"
                                      CanContentScroll="True">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ListBox.Template>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsVirtualizing="True" VirtualizationMode="Recycling"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemBorder" Background="Transparent" Padding="0" Margin="0">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SelectedBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="2" CornerRadius="6" Background="Transparent">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding FileIcon}" FontSize="16" VerticalAlignment="Top" Margin="0,2,8,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding FileName}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding RelativePath}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                    <TextBlock Text="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="  •  " FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{Binding SizeDisplay}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="  •  " FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{Binding Extension}" FontSize="10" Foreground="{DynamicResource AccentBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Margin="2,8,2,0">
            <ProgressBar x:Name="ScanningProgress"
                         Height="3"
                         IsIndeterminate="True"
                         Foreground="{DynamicResource AccentBrush}"
                         Background="{DynamicResource CardBorderBrush}"
                         Visibility="Collapsed"/>
            <TextBlock x:Name="StatusText"
                      Text="Select a project to begin searching."
                      FontSize="10"
                      Foreground="{DynamicResource TextSecondaryBrush}"
                      Margin="0,6,0,0"
                      TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
