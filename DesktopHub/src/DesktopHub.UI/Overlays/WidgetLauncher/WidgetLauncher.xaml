<Window x:Class="DesktopHub.UI.WidgetLauncher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Widgets"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        Width="180"
        SizeToContent="Height"
        MaxHeight="400"
        Deactivated="Window_Deactivated"
        KeyDown="Window_KeyDown">
    
    <Border x:Name="RootBorder"
            Background="{DynamicResource WindowBackgroundAlphaBrush}"
            CornerRadius="12"
            BorderBrush="{DynamicResource WindowBorderBrush}"
            BorderThickness="1">
        <Grid>
            <!-- Glass overlay effect -->
            <Border Style="{StaticResource GlassOverlayBorder}"/>
            
            <!-- Content -->
            <Border Background="Transparent" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header with close button -->
                    <Grid Grid.Row="0" Margin="0,0,0,14">
                        <TextBlock Text="Widgets"
                                  FontSize="13"
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource TextPrimaryBrush}"
                                  VerticalAlignment="Center"/>
                        <Border HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Width="20" Height="20"
                                CornerRadius="4"
                                Background="Transparent"
                                Cursor="Hand"
                                MouseLeftButtonDown="CloseButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverStrongBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="✕"
                                      FontSize="10"
                                      Foreground="{DynamicResource TextSecondaryBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    
                    <!-- Widget buttons -->
                    <ScrollViewer x:Name="WidgetButtonsScroller"
                                  Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Background="Transparent"
                                  Focusable="False">
                        <ScrollViewer.Resources>
                            <Style x:Key="LauncherScrollThumbStyle" TargetType="Thumb">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="{DynamicResource ScrollbarThumbBrush}" CornerRadius="3"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>

                            <Style TargetType="ScrollBar">
                                <Setter Property="Width" Value="5"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ScrollBar">
                                            <Grid Background="Transparent" Width="5">
                                                <Track x:Name="PART_Track" IsDirectionReversed="True">
                                                    <Track.DecreaseRepeatButton>
                                                        <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.DecreaseRepeatButton>
                                                    <Track.Thumb>
                                                        <Thumb Style="{StaticResource LauncherScrollThumbStyle}"/>
                                                    </Track.Thumb>
                                                    <Track.IncreaseRepeatButton>
                                                        <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" IsHitTestVisible="False"/>
                                                    </Track.IncreaseRepeatButton>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ScrollViewer.Resources>
                        <ScrollViewer.Style>
                            <Style TargetType="ScrollViewer">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ScrollViewer">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="0"/>
                                                </Grid.ColumnDefinitions>
                                                <ScrollContentPresenter Grid.Column="0"/>
                                                <ScrollBar x:Name="PART_VerticalScrollBar"
                                                           Grid.Column="1"
                                                           Width="5"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Stretch"
                                                           Value="{TemplateBinding VerticalOffset}"
                                                           Maximum="{TemplateBinding ScrollableHeight}"
                                                           ViewportSize="{TemplateBinding ViewportHeight}"
                                                           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                                           Background="Transparent"
                                                           Opacity="0"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <EventTrigger RoutedEvent="MouseEnter">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar"
                                                                             Storyboard.TargetProperty="Opacity"
                                                                             To="1" Duration="0:0:0.15"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                                <EventTrigger RoutedEvent="MouseLeave">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar"
                                                                             Storyboard.TargetProperty="Opacity"
                                                                             To="0" Duration="0:0:0.4" BeginTime="0:0:0.6"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ScrollViewer.Style>
                    <StackPanel>
                        <Border x:Name="SearchWidgetButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="SearchWidgetButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔍" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Search" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="TimerWidgetButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="TimerWidgetButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⏱️" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Timer" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="QuickTasksWidgetButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="QuickTasksWidgetButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#9745;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Quick Tasks" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <Border x:Name="FrequentProjectsButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="FrequentProjectsButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#9733;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Frequent Projects" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="QuickLaunchButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="QuickLaunchButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#128640;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Quick Launch" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="DocQuickOpenButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="DocQuickOpenButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#128196;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Doc Quick Open" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="SmartProjectSearchButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="SmartProjectSearchButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#128269;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Smart Search" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="MetricsViewerButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                Visibility="Collapsed"
                                MouseLeftButtonDown="MetricsViewerButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#128202;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Metrics" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="ProjectInfoButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                Visibility="Collapsed"
                                MouseLeftButtonDown="ProjectInfoButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🏷️" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Project Info" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="DeveloperPanelButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                Visibility="Collapsed"
                                MouseLeftButtonDown="DeveloperPanelButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🛠️" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Developer Panel" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="CheatSheetButton"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource SelectedBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8"
                                Cursor="Hand"
                                MouseLeftButtonDown="CheatSheetButton_Click">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverMediumBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#128218;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Cheat Sheets" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Update available indicator -->
            <Ellipse x:Name="UpdateIndicator"
                     Width="10" Height="10"
                     Fill="{DynamicResource GreenBrush}"
                     HorizontalAlignment="Right"
                     VerticalAlignment="Bottom"
                     Margin="0,0,8,8"
                     Visibility="Collapsed"
                     IsHitTestVisible="False"/>
        </Grid>
    </Border>
</Window>
