<Window x:Class="CoffeeStockWidget.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="180" Width="320"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False"
        Foreground="#FFFFFFFF" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border CornerRadius="12" Background="#FF1E1E1E" BorderBrush="#33000000" BorderThickness="1" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Check every (seconds):" VerticalAlignment="Center" FontSize="13"/>
                <TextBox x:Name="IntervalBox" Width="90" Margin="8,0,0,0"/>
            </StackPanel>

            <CheckBox x:Name="RunAtLoginBox" Grid.Row="1" Content="Run at login" FontSize="13"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="CancelBtn" Content="Cancel" Width="80" Margin="0,0,8,0" Click="CancelBtn_Click"/>
                <Button x:Name="SaveBtn" Content="Save" Width="80" Click="SaveBtn_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
